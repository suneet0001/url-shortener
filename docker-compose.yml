version: "3"

services:

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb://host.docker.internal:27017/url-shortener

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend